language: rust
rust:
  - nightly
  - stable
  - beta
services:
  - redis-server
script:
  - cargo test --workspace
  - cargo test --workspace --no-default-features --features=runtime_agnostic
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
